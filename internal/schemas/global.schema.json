{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/LucasPcq/worktree-manager-cli/main/schemas/global.schema.json",
  "title": "wtm global config",
  "description": "Schema for ~/.config/wtm/config.toml — per-user defaults applied across all projects.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "shell": {
      "type": "string",
      "enum": ["zsh", "bash", "fish"],
      "description": "Shell wrapper generated by `wtm shell init`. The wrapper enables `wt go` / `wt switch` to change the working directory."
    },
    "ui": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "animations": {
          "type": "boolean",
          "description": "Whether `wtm ui` animates its tab rule and a newly created row. Absent means on; set to false to disable every dashboard animation at once."
        }
      }
    },
    "proxy": {
      "type": "object",
      "additionalProperties": false,
      "description": "The run proxy, which serves each worktree's HTTP jobs under their own hostname so two worktrees stop sharing one cookie jar. It lives in the background daemon and dies with it — a job only gets a name if its `[[job]]` declares a `url` in run.toml.",
      "properties": {
        "port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "description": "Port the proxy listens on, on the loopback only. Absent means 4000. The port is a property of the machine, not of a repository: one daemon serves every repo at once."
        },
        "enabled": {
          "type": "boolean",
          "description": "Absent means on. Set to false to switch the proxy off: jobs keep their own ports, and every URL wtm prints falls back to `http://localhost:<port>`."
        }
      }
    }
  }
}
