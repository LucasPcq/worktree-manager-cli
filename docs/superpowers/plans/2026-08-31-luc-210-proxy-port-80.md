# LUC-210 — Des URLs nommées sans port : plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** servir le proxy `run` sur le port 80 via une redirection OS installable, pour que `http://web.feat-x.monorepo.localhost/` remplace `http://web.feat-x.monorepo.localhost:4000/`, sans jamais faire tourner le daemon en root et sans casser le cas « pas installé ».

**Architecture:** on sépare le **port de bind** (ce que le proxy écoute, non privilégié) du **port public** (ce qui apparaît dans l'URL). Le port public vaut 80 si et seulement si une sonde prouve que notre proxy répond derrière le 80 ; hors daemon, l'état déclaré par l'installation prend le relais. La redirection est posée par `wtm run proxy install` derrière une interface `Redirector` à une implémentation par plateforme (macOS : ancre `pf` + `LaunchDaemon`, modèle Pow/Valet).

**Tech Stack:** Go 1.x, cobra, bubbletea/lipgloss (surfaces uniquement), `net/http` + `httputil.ReverseProxy`, `pfctl`/`launchctl` sur darwin.

**Spec:** `docs/superpowers/specs/2026-08-31-luc-210-proxy-port-80-design.md`

## Global Constraints

Ces contraintes viennent de `CLAUDE.md` et de la spec. Elles s'appliquent à **toutes** les tâches.

- Toute fonction ou méthode à 2 paramètres liés ou plus prend **une struct**, initialisée en champs nommés.
- Toute chaîne, tout nombre magique, tout nom de flag va dans `internal/domain/constants.go` ; les erreurs sentinelles dans `internal/domain/errors.go`.
- `internal/rules/` n'importe que la stdlib et `internal/domain` — aucune I/O, aucun effet de bord.
- `internal/service/` n'importe jamais `cobra`, `bubbletea` ni `lipgloss`.
- `internal/output/` et `internal/tui/` n'ont aucune logique de décision ; `internal/styles/` est le seul package qui instancie un `lipgloss.Style`.
- Retours anticipés systématiques, aucun `if` imbriqué, chemin heureux en dernier.
- Assertions de type toujours en comma-ok.
- **Commentaires quasi nuls** : le pourquoi, jamais le quoi. Les blocs de commentaires donnés dans ce plan sont ceux à garder, n'en ajoutez pas d'autres. En touchant un fichier, supprimez-y les commentaires qui redisent le code.
- Ces commandes **ne passent pas par `internal/flow/`** : la règle vise les commandes qui mutent un worktree, celle-ci mute la machine.
- Axe `--yes` respecté (confirmation), **pas de `--force`** (aucun refus de sécurité à lever).
- Encadrement vertical : une commande encadre sa sortie humaine **exactement une fois** avec `output.Frame` ; le JSON n'est jamais encadré.
- **Aucun test n'exécute `sudo`, ne lit un fichier système, ni ne bind un port privilégié.** Le port sondé et la racine du système de fichiers sont des paramètres.
- Après la dernière tâche : `make docs`, la ligne README, `internal/commands/agents/assets/using-wtm.skill.md`, puis le sous-agent `build-validator`.
- Commits fréquents, un par tâche minimum. Pas d'attribution de co-auteur.

**Valeurs exactes à reprendre telles quelles :**

- port privilégié visé : `80`
- hôte sentinelle de la sonde : `probe.wtm.localhost`
- en-tête de la sonde : `X-Wtm-Proxy`
- timeout de la sonde : `150ms`
- nom de l'ancre pf : `wtm`
- chemins : `/etc/pf.anchors/wtm`, `/etc/pf.conf`, `/Library/LaunchDaemons/dev.wtm.proxy.plist`
- label launchd : `dev.wtm.proxy`

## Structure des fichiers

**Créés :**

| Fichier | Responsabilité |
|---|---|
| `internal/rules/publicport.go` | la règle pure des trois étages de résolution du port public |
| `internal/rules/publicport_test.go` | ses tests |
| `internal/rules/pfconf.go` | insertion/retrait purs du bloc balisé dans un `pf.conf` |
| `internal/rules/pfconf_test.go` | ses tests |
| `internal/rules/proxyhint.go` | les lignes du callout de `run init` |
| `internal/rules/proxyhint_test.go` | ses tests |
| `internal/service/proxy/probe.go` | la sonde HTTP sur le port privilégié |
| `internal/service/proxy/probe_test.go` | ses tests |
| `internal/service/proxy/redirect.go` | l'interface `Redirector`, les types `Plan`/`Status`, le sélecteur de plateforme |
| `internal/service/proxy/redirect_darwin.go` | l'implémentation pf + launchd |
| `internal/service/proxy/redirect_darwin_test.go` | ses tests (rendu et inspection seulement) |
| `internal/service/proxy/redirect_other.go` | le refus honnête hors darwin |
| `internal/commands/run/proxycmd/proxycmd.go` | le groupe `wtm run proxy` |
| `internal/commands/run/proxycmd/status.go` | `status` |
| `internal/commands/run/proxycmd/install.go` | `install` et `uninstall` |
| `internal/commands/run/proxycmd/status_test.go` | forme du JSON, refus hors TTY |
| `internal/output/proxy.go` | rendu texte et JSON du statut |

**Modifiés :**

| Fichier | Modification |
|---|---|
| `internal/domain/constants.go` | les constantes listées plus haut |
| `internal/domain/errors.go` | 3 erreurs sentinelles |
| `internal/domain/proxy.go` | le type `ProxyStatus` |
| `internal/rules/joburl.go` | `JobOrigin` + `JobURL` réécrit par-dessus |
| `internal/service/proxy/server.go` | la route sentinelle |
| `internal/service/process/protocol.go` | `Response.ProxyPublicPort` |
| `internal/service/process/daemon.go` | la sonde au démarrage, le champ, les 4 encodages |
| `internal/commands/run/url.go` | la résolution à trois étages |
| `internal/commands/run/start.go` | passe le port public |
| `internal/flow/runlogs/run.go` | passe le port public |
| `internal/commands/run/run.go` | `cmd.AddCommand(proxycmd.NewCmd())` |
| `internal/commands/run/init.go` | le callout d'épilogue |

---

### Task 1 : `rules.JobOrigin` — l'URL perd son port sur le 80

**Files:**
- Modify: `internal/domain/constants.go`
- Modify: `internal/rules/joburl.go:9-36`
- Test: `internal/rules/joburl_test.go`

**Interfaces:**
- Consumes: rien.
- Produces: `rules.JobOrigin(rules.JobOriginParams{Host string, PublicPort int, DirectPort int}) string` ; `rules.JobURLParams.PublicPort` (renommage de `ProxyPort`) ; `domain.ProxyPrivilegedPort = 80` ; `domain.ProxyOriginFmt = "http://%s"`.

- [ ] **Step 1: Écrire les cas de test qui échouent**

Dans `internal/rules/joburl_test.go`, ajoutez deux cas à la table de `TestJobURL` (renommez le champ `proxyPort` de la struct de test en `publicPort` et l'appel en conséquence) :

```go
{
	name:       "port public 80 : l'URL ne porte aucun port",
	job:        web,
	ports:      map[string]int{"PORT": 3010},
	host:       "web.feat-auth.myapp.localhost",
	publicPort: 80,
	want:       "http://web.feat-auth.myapp.localhost",
},
{
	name:       "port public haut : le port reste dans l'URL",
	job:        web,
	ports:      map[string]int{"PORT": 3010},
	host:       "web.feat-auth.myapp.localhost",
	publicPort: 4001,
	want:       "http://web.feat-auth.myapp.localhost:4001",
},
```

Puis un test dédié à la nouvelle fonction :

```go
func TestJobOrigin(t *testing.T) {
	tests := []struct {
		name       string
		host       string
		publicPort int
		directPort int
		want       string
	}{
		{name: "nom sur le 80", host: "web.f.app.localhost", publicPort: 80, directPort: 3010, want: "http://web.f.app.localhost"},
		{name: "nom sur un port haut", host: "web.f.app.localhost", publicPort: 4000, directPort: 3010, want: "http://web.f.app.localhost:4000"},
		{name: "pas de proxy : la forme directe", host: "web.f.app.localhost", directPort: 3010, want: "http://localhost:3010"},
		{name: "pas d'hôte : la forme directe", publicPort: 80, directPort: 3010, want: "http://localhost:3010"},
		{name: "rien à publier", want: ""},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got := JobOrigin(JobOriginParams{Host: tt.host, PublicPort: tt.publicPort, DirectPort: tt.directPort})
			if got != tt.want {
				t.Errorf("got %q, want %q", got, tt.want)
			}
		})
	}
}
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run: `go test ./internal/rules/ -run 'TestJobURL|TestJobOrigin' -v`
Expected: FAIL — `undefined: JobOrigin`, `unknown field publicPort`.

- [ ] **Step 3: Ajouter les constantes**

Dans `internal/domain/constants.go`, à côté de `ProxyURLFmt` :

```go
	// ProxyPrivilegedPort is the port a named URL loses: reached through an OS
	// redirection the daemon never binds itself.
	ProxyPrivilegedPort = 80
	// ProxyOriginFmt is a named URL served on the privileged port.
	ProxyOriginFmt = "http://%s"
```

- [ ] **Step 4: Écrire l'implémentation minimale**

Dans `internal/rules/joburl.go`, renommez `JobURLParams.ProxyPort` en `PublicPort` et remplacez le corps de `JobURL` :

```go
type JobOriginParams struct {
	Host string
	// PublicPort is what the URL announces, not what the proxy binds:
	// ProxyPrivilegedPort means the redirection is live and the port vanishes.
	PublicPort int
	DirectPort int
}

func JobOrigin(params JobOriginParams) string {
	if params.Host != "" && params.PublicPort == domain.ProxyPrivilegedPort {
		return fmt.Sprintf(domain.ProxyOriginFmt, params.Host)
	}
	if params.Host != "" && params.PublicPort > 0 {
		return fmt.Sprintf(domain.ProxyURLFmt, params.Host, params.PublicPort)
	}
	if params.DirectPort > 0 {
		return fmt.Sprintf(domain.DirectURLFmt, params.DirectPort)
	}
	return ""
}

func JobURL(params JobURLParams) string {
	if params.Job.URL == nil {
		return ""
	}
	port, bound := params.Ports[params.Job.URL.Port]
	if !bound {
		return ""
	}
	return JobOrigin(JobOriginParams{Host: params.Host, PublicPort: params.PublicPort, DirectPort: port})
}
```

- [ ] **Step 5: Répercuter le renommage sur les 4 appelants**

Run: `grep -rn "ProxyPort:" internal --include "*.go" | grep JobURLParams -A0` puis remplacez `ProxyPort:` par `PublicPort:` dans `internal/commands/run/url.go:86`, `internal/commands/run/start.go:144`, `internal/service/process/daemon.go:279`, `internal/flow/runlogs/run.go:246`. La **valeur** passée ne change pas encore : la sémantique bascule en Task 6.

- [ ] **Step 6: Lancer les tests**

Run: `go build ./... && go test ./internal/rules/ -run 'TestJobURL|TestJobOrigin' -v`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add internal/domain/constants.go internal/rules/joburl.go internal/rules/joburl_test.go internal/commands/run/url.go internal/commands/run/start.go internal/service/process/daemon.go internal/flow/runlogs/run.go
git commit -m "feat(run): une origine de job qui perd son port sur le 80 (LUC-210)"
```

---

### Task 2 : la route sentinelle du proxy

**Files:**
- Modify: `internal/domain/constants.go`
- Modify: `internal/service/proxy/server.go:79-95`
- Test: `internal/service/proxy/server_test.go`

**Interfaces:**
- Consumes: Task 1 (rien de direct).
- Produits: `domain.ProxyProbeHost = "probe.wtm.localhost"`, `domain.ProxyProbeHeader = "X-Wtm-Proxy"` ; le serveur répond `204` + `X-Wtm-Proxy: <port de bind>` sur cet hôte.

- [ ] **Step 1: Écrire le test qui échoue**

Dans `internal/service/proxy/server_test.go` :

```go
func TestServerAnswersTheProbeWithItsBindPort(t *testing.T) {
	addr := serve(t)

	resp := get(t, addr, domain.ProxyProbeHost)

	if resp.StatusCode != http.StatusNoContent {
		t.Errorf("status: got %d, want %d", resp.StatusCode, http.StatusNoContent)
	}
	_, port, err := net.SplitHostPort(addr)
	if err != nil {
		t.Fatal(err)
	}
	if got := resp.Header.Get(domain.ProxyProbeHeader); got != port {
		t.Errorf("header: got %q, want %q", got, port)
	}
}

func TestServerProbeIgnoresTheRegistry(t *testing.T) {
	addr := serve(t, domain.ProxyRoute{Host: domain.ProxyProbeHost, Target: "127.0.0.1:1", Job: "web"})

	resp := get(t, addr, domain.ProxyProbeHost)

	if resp.StatusCode != http.StatusNoContent {
		t.Errorf("une route enregistrée sous l'hôte sentinelle ne doit pas être servie : got %d", resp.StatusCode)
	}
}
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `go test ./internal/service/proxy/ -run TestServerProbe -v` et `go test ./internal/service/proxy/ -run TestServerAnswersTheProbe -v`
Expected: FAIL — `undefined: domain.ProxyProbeHost`.

- [ ] **Step 3: Ajouter les constantes**

Dans `internal/domain/constants.go` :

```go
	// ProxyProbeHost and ProxyProbeHeader are how a caller proves the proxy
	// answering behind the privileged port is this one: the header carries the
	// bind port, so a stale redirection is a mismatch rather than a false yes.
	ProxyProbeHost   = "probe.wtm.localhost"
	ProxyProbeHeader = "X-Wtm-Proxy"
```

- [ ] **Step 4: Écrire l'implémentation minimale**

Dans `internal/service/proxy/server.go`, en tête de `route`, après l'extraction du host :

```go
	if host == domain.ProxyProbeHost {
		w.Header().Set(domain.ProxyProbeHeader, strconv.Itoa(s.port))
		w.WriteHeader(http.StatusNoContent)
		return
	}
```

Ajoutez `"strconv"` aux imports.

- [ ] **Step 5: Lancer les tests**

Run: `go test ./internal/service/proxy/ -v`
Expected: PASS, y compris les tests existants.

- [ ] **Step 6: Commit**

```bash
git add internal/domain/constants.go internal/service/proxy/server.go internal/service/proxy/server_test.go
git commit -m "feat(run): le proxy répond à une sonde qui nomme son port de bind (LUC-210)"
```

---

### Task 3 : la sonde

**Files:**
- Create: `internal/service/proxy/probe.go`
- Create: `internal/service/proxy/probe_test.go`
- Modify: `internal/domain/constants.go`

**Interfaces:**
- Consumes: Task 2 (`domain.ProxyProbeHost`, `domain.ProxyProbeHeader`, la route sentinelle).
- Produces: `proxy.Probe(proxy.ProbeParams{Port int, Timeout time.Duration}) int` — le port de bind vu derrière `Port`, zéro quand ce n'est pas notre proxy.

- [ ] **Step 1: Écrire le test qui échoue**

`internal/service/proxy/probe_test.go` :

```go
package proxy

import (
	"net"
	"net/http"
	"net/http/httptest"
	"strconv"
	"testing"

	"github.com/LucasPcq/wtm/internal/domain"
)

func portOf(t *testing.T, addr string) int {
	t.Helper()
	_, port, err := net.SplitHostPort(addr)
	if err != nil {
		t.Fatal(err)
	}
	n, err := strconv.Atoi(port)
	if err != nil {
		t.Fatal(err)
	}
	return n
}

func TestProbeSeesOurOwnProxy(t *testing.T) {
	addr := serve(t)
	port := portOf(t, addr)

	if got := Probe(ProbeParams{Port: port}); got != port {
		t.Errorf("got %d, want %d", got, port)
	}
}

func TestProbeIgnoresAnotherServer(t *testing.T) {
	other := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, _ *http.Request) {
		w.WriteHeader(http.StatusNoContent)
	}))
	t.Cleanup(other.Close)

	if got := Probe(ProbeParams{Port: portOf(t, other.Listener.Addr().String())}); got != 0 {
		t.Errorf("un serveur sans l'en-tête %s n'est pas le nôtre : got %d", domain.ProxyProbeHeader, got)
	}
}

func TestProbeOnAClosedPortReturnsZero(t *testing.T) {
	listener, err := net.Listen("tcp", "127.0.0.1:0")
	if err != nil {
		t.Fatal(err)
	}
	port := portOf(t, listener.Addr().String())
	listener.Close()

	if got := Probe(ProbeParams{Port: port}); got != 0 {
		t.Errorf("got %d, want 0", got)
	}
}
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run: `go test ./internal/service/proxy/ -run TestProbe -v`
Expected: FAIL — `undefined: Probe`.

- [ ] **Step 3: Ajouter la constante de timeout**

Dans `internal/domain/constants.go` :

```go
	// ProxyProbeTimeoutMs bounds the probe: nothing listening refuses at once,
	// so this only ever pays for a host that accepts and then stalls.
	ProxyProbeTimeoutMs = 150
```

- [ ] **Step 4: Écrire l'implémentation**

`internal/service/proxy/probe.go` :

```go
package proxy

import (
	"fmt"
	"net/http"
	"strconv"
	"time"

	"github.com/LucasPcq/wtm/internal/domain"
)

type ProbeParams struct {
	Port int
	// Timeout takes the default when zero.
	Timeout time.Duration
}

// Probe is which bind port answers behind Port, zero when what answers is not
// this proxy. The bind port in the reply is what separates a live redirection
// from one still pointing at a port the daemon no longer holds.
func Probe(params ProbeParams) int {
	timeout := params.Timeout
	if timeout <= 0 {
		timeout = time.Duration(domain.ProxyProbeTimeoutMs) * time.Millisecond
	}

	req, err := http.NewRequest(http.MethodGet, fmt.Sprintf("http://"+domain.ProxyLoopbackFmt+"/", params.Port), nil)
	if err != nil {
		return 0
	}
	req.Host = domain.ProxyProbeHost

	resp, err := (&http.Client{Timeout: timeout}).Do(req)
	if err != nil {
		return 0
	}
	defer resp.Body.Close()

	port, err := strconv.Atoi(resp.Header.Get(domain.ProxyProbeHeader))
	if err != nil {
		return 0
	}
	return port
}
```

- [ ] **Step 5: Lancer les tests**

Run: `go test ./internal/service/proxy/ -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/domain/constants.go internal/service/proxy/probe.go internal/service/proxy/probe_test.go
git commit -m "feat(run): une sonde qui prouve que le 80 mène à notre proxy (LUC-210)"
```

---

### Task 4 : la règle des trois étages

**Files:**
- Create: `internal/rules/publicport.go`
- Create: `internal/rules/publicport_test.go`

**Interfaces:**
- Consumes: `domain.ProxyPrivilegedPort` (Task 1).
- Produces: `rules.PublicPort(rules.PublicPortParams{BindPort int, Probed int, Declared bool, DaemonUp bool}) int`.

- [ ] **Step 1: Écrire le test qui échoue**

`internal/rules/publicport_test.go` :

```go
package rules

import "testing"

func TestPublicPort(t *testing.T) {
	tests := []struct {
		name     string
		bindPort int
		probed   int
		declared bool
		daemonUp bool
		want     int
	}{
		{name: "proxy éteint", want: 0},
		{name: "daemon up, sonde concordante : le 80", bindPort: 4000, probed: 4000, daemonUp: true, want: 80},
		{name: "daemon up, rien derrière le 80", bindPort: 4000, daemonUp: true, want: 4000},
		{name: "daemon up, redirection périmée", bindPort: 4001, probed: 4000, daemonUp: true, want: 4001},
		{name: "daemon up : la sonde prime sur le déclaré", bindPort: 4000, declared: true, daemonUp: true, want: 4000},
		{name: "hors daemon, redirection déclarée", bindPort: 4000, declared: true, want: 80},
		{name: "hors daemon, rien de déclaré", bindPort: 4000, want: 4000},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got := PublicPort(PublicPortParams{BindPort: tt.bindPort, Probed: tt.probed, Declared: tt.declared, DaemonUp: tt.daemonUp})
			if got != tt.want {
				t.Errorf("got %d, want %d", got, tt.want)
			}
		})
	}
}
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `go test ./internal/rules/ -run TestPublicPort -v`
Expected: FAIL — `undefined: PublicPort`.

- [ ] **Step 3: Écrire l'implémentation**

`internal/rules/publicport.go` :

```go
package rules

import "github.com/LucasPcq/wtm/internal/domain"

type PublicPortParams struct {
	// BindPort is what the proxy really listens on, zero when it is off.
	BindPort int
	// Probed is what answered behind the privileged port, zero when nothing of
	// ours did. Only read when DaemonUp.
	Probed   int
	Declared bool
	DaemonUp bool
}

// PublicPort is the port a named URL announces. A running daemon settles it by
// probe; without one the installation's declared state answers, which is what
// lets a worktree being created write an origin with no daemon in sight.
func PublicPort(params PublicPortParams) int {
	if params.BindPort == 0 {
		return 0
	}
	if params.DaemonUp {
		if params.Probed == params.BindPort {
			return domain.ProxyPrivilegedPort
		}
		return params.BindPort
	}
	if params.Declared {
		return domain.ProxyPrivilegedPort
	}
	return params.BindPort
}
```

- [ ] **Step 4: Lancer le test**

Run: `go test ./internal/rules/ -run TestPublicPort -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/rules/publicport.go internal/rules/publicport_test.go
git commit -m "feat(run): la règle des trois étages du port public (LUC-210)"
```

---

### Task 5 : le daemon sonde et publie le port public

**Files:**
- Modify: `internal/service/process/protocol.go:56-70`
- Modify: `internal/service/process/daemon.go:30-76`, `:191`, `:211`, `:220`, `:267`, `:279-285`

**Interfaces:**
- Consumes: `proxy.Probe` (Task 3), `rules.PublicPort` (Task 4).
- Produces: `process.Response.ProxyPublicPort int` (`json:"proxy_public_port,omitempty"`) — le port public résolu par le daemon.

- [ ] **Step 1: Ajouter le champ au protocole**

Dans `internal/service/process/protocol.go`, sous `ProxyPort` :

```go
	// ProxyPublicPort is what a named URL announces: ProxyPrivilegedPort when
	// the probe reached us behind it, ProxyPort otherwise.
	ProxyPublicPort int `json:"proxy_public_port,omitempty"`
```

- [ ] **Step 2: Sonder au démarrage**

Dans `internal/service/process/daemon.go`, ajoutez le champ à `daemonServer` :

```go
	proxyPublicPort int
```

et, dans `RunDaemon`, dans la branche `else` qui suit `server.Start()` :

```go
		} else {
			d.proxyPort = server.Port()
			d.proxyPublicPort = rules.PublicPort(rules.PublicPortParams{
				BindPort: server.Port(),
				Probed:   proxy.Probe(proxy.ProbeParams{Port: domain.ProxyPrivilegedPort}),
				DaemonUp: true,
			})
			defer server.Close()
		}
```

- [ ] **Step 3: Publier le champ dans les quatre réponses**

Ajoutez `ProxyPublicPort: d.proxyPublicPort` aux `Response{…}` des lignes 191, 211, 220 et 267 (à côté de `ProxyPort: d.proxyPort`), et remplacez `PublicPort:` dans le `rules.JobURLParams` de la ligne ~279 par `PublicPort: d.proxyPublicPort`.

- [ ] **Step 4: Vérifier la compilation et la non-régression**

Run: `go build ./... && go test ./internal/service/... ./internal/commands/run/... -count=1`
Expected: PASS. Le daemon sonde le 80 : rien n'y écoute en test, la sonde renvoie 0 et le port public retombe sur le port de bind — comportement identique à l'actuel.

- [ ] **Step 5: Commit**

```bash
git add internal/service/process/protocol.go internal/service/process/daemon.go
git commit -m "feat(run): le daemon résout et publie le port public du proxy (LUC-210)"
```

---

### Task 6 : la résolution côté client

**Files:**
- Modify: `internal/commands/run/url.go:75-110`
- Modify: `internal/commands/run/start.go:144`
- Modify: `internal/flow/runlogs/run.go:246`
- Test: `internal/commands/run/url_test.go`

**Interfaces:**
- Consumes: `rules.PublicPort` (Task 4), `Response.ProxyPublicPort` (Task 5).
- Produces: `run.publicProxyPort(configured int) int` (non exporté, dans `url.go`) — remplace `servedProxyPort`.

> **Note d'ordonnancement :** l'étage « déclaré » a besoin de `proxy.Inspect()` (Task 8). Cette tâche câble les étages 1 et 3 et laisse `Declared: false` ; la Task 8 y branche l'inspection en une ligne. La tâche reste verte et testable seule.

- [ ] **Step 1: Écrire le test qui échoue**

Dans `internal/commands/run/url_test.go`, ajoutez :

```go
func TestPublicProxyPortWithoutDaemon(t *testing.T) {
	if got := publicProxyPort(4000); got != 4000 {
		t.Errorf("sans daemon ni redirection déclarée, le port de bind est la réponse : got %d", got)
	}
	if got := publicProxyPort(0); got != 0 {
		t.Errorf("proxy éteint : got %d, want 0", got)
	}
}
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `go test ./internal/commands/run/ -run TestPublicProxyPort -v`
Expected: FAIL — `undefined: publicProxyPort`.

- [ ] **Step 3: Remplacer `servedProxyPort`**

Dans `internal/commands/run/url.go`, remplacez la fonction et son appel :

```go
// publicProxyPort is what a named URL announces here. A running daemon has
// already probed, so its answer is taken whole; without one the installation's
// declared state is all there is to go on.
func publicProxyPort(configured int) int {
	if configured == 0 {
		return 0
	}

	socketPath := process.SocketPath()
	if process.IsDaemonRunning(socketPath) {
		resp, err := process.NewClient(socketPath).Send(process.Request{Action: process.ActionList})
		if err == nil && resp.Status != process.StatusError {
			return resp.ProxyPublicPort
		}
	}
	return rules.PublicPort(rules.PublicPortParams{BindPort: configured, Declared: false})
}
```

Dans `publishedJobs`, remplacez `proxyPort := servedProxyPort(rules.ProxyPort(result.Config.Global))` par `proxyPort := publicProxyPort(rules.ProxyPort(result.Config.Global))`.

- [ ] **Step 4: Câbler les deux autres appelants**

Dans `internal/commands/run/start.go:144` et `internal/flow/runlogs/run.go:246`, la valeur passée à `PublicPort:` doit venir de la réponse du daemon : remplacez la lecture de `resp.ProxyPort` par `resp.ProxyPublicPort` au point où cette valeur est obtenue. Run `grep -n "ProxyPort" internal/commands/run/start.go internal/flow/runlogs/run.go` pour localiser exactement les deux lectures.

- [ ] **Step 5: Lancer les tests**

Run: `go build ./... && go test ./internal/commands/run/... ./internal/flow/... -count=1`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add internal/commands/run/url.go internal/commands/run/url_test.go internal/commands/run/start.go internal/flow/runlogs/run.go
git commit -m "feat(run): les surfaces annoncent le port public, pas le port de bind (LUC-210)"
```

---

### Task 7 : le bloc balisé de `pf.conf`

**Files:**
- Create: `internal/rules/pfconf.go`
- Create: `internal/rules/pfconf_test.go`
- Modify: `internal/domain/constants.go`, `internal/domain/errors.go`

**Interfaces:**
- Consumes: rien.
- Produces: `rules.PfConfWithWTM(content string) (string, error)`, `rules.PfConfWithoutWTM(content string) string`, `rules.PfConfHasWTM(content string) bool` ; `domain.ErrPfConfUnrecognised`.

**Contexte pour l'implémenteur :** `pf` impose l'ordre des types de règles — options, normalisation, mise en file, **traduction**, filtrage. Un `rdr-anchor` placé après les règles de filtre fait échouer `pfctl`. Les instructions `load anchor`, elles, vont en fin de fichier (c'est ce que fait Apple avec `com.apple`). D'où **deux** blocs balisés, à deux endroits.

- [ ] **Step 1: Écrire les tests qui échouent**

`internal/rules/pfconf_test.go` :

```go
package rules

import (
	"errors"
	"strings"
	"testing"

	"github.com/LucasPcq/wtm/internal/domain"
)

// applePfConf is the stock macOS /etc/pf.conf, which is what install meets on
// an untouched machine.
const applePfConf = `#
# Default PF configuration file.
#
scrub-anchor "com.apple/*"
nat-anchor "com.apple/*"
rdr-anchor "com.apple/*"
dummynet-anchor "com.apple/*"
anchor "com.apple/*"
load anchor "com.apple" from "/etc/pf.anchors/com.apple"
`

func TestPfConfInsertsBothBlocksInOrder(t *testing.T) {
	got, err := PfConfWithWTM(applePfConf)
	if err != nil {
		t.Fatal(err)
	}

	rdr := strings.Index(got, "rdr-anchor \"wtm\"")
	load := strings.Index(got, "load anchor \"wtm\"")
	filter := strings.Index(got, "anchor \"com.apple/*\"")
	appleLoad := strings.Index(got, "load anchor \"com.apple\"")

	if rdr == -1 || load == -1 {
		t.Fatalf("les deux lignes doivent être présentes:\n%s", got)
	}
	if rdr > filter {
		t.Error("rdr-anchor doit précéder les règles de filtre")
	}
	if load < appleLoad {
		t.Error("load anchor va en fin de fichier")
	}
}

func TestPfConfInsertIsIdempotent(t *testing.T) {
	once, err := PfConfWithWTM(applePfConf)
	if err != nil {
		t.Fatal(err)
	}
	twice, err := PfConfWithWTM(once)
	if err != nil {
		t.Fatal(err)
	}
	if once != twice {
		t.Errorf("une seconde insertion ne doit rien changer:\n%s", twice)
	}
}

func TestPfConfRemovalRestoresTheOriginalByteForByte(t *testing.T) {
	with, err := PfConfWithWTM(applePfConf)
	if err != nil {
		t.Fatal(err)
	}
	if got := PfConfWithoutWTM(with); got != applePfConf {
		t.Errorf("got:\n%q\nwant:\n%q", got, applePfConf)
	}
}

func TestPfConfRemovalOnAVirginFileIsANoOp(t *testing.T) {
	if got := PfConfWithoutWTM(applePfConf); got != applePfConf {
		t.Errorf("got:\n%q", got)
	}
}

func TestPfConfKeepsHandWrittenRulesAround(t *testing.T) {
	custom := applePfConf + "block in proto tcp from any to any port 23\n"
	with, err := PfConfWithWTM(custom)
	if err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(with, "block in proto tcp from any to any port 23") {
		t.Error("une règle écrite à la main ne doit pas disparaître")
	}
	if got := PfConfWithoutWTM(with); got != custom {
		t.Errorf("le retrait doit rendre le fichier de départ:\n%q", got)
	}
}

func TestPfConfRefusesAFileWithNoTranslationSection(t *testing.T) {
	if _, err := PfConfWithWTM("set skip on lo0\n"); !errors.Is(err, domain.ErrPfConfUnrecognised) {
		t.Errorf("got %v, want ErrPfConfUnrecognised", err)
	}
}

func TestPfConfHasWTM(t *testing.T) {
	if PfConfHasWTM(applePfConf) {
		t.Error("fichier vierge")
	}
	with, err := PfConfWithWTM(applePfConf)
	if err != nil {
		t.Fatal(err)
	}
	if !PfConfHasWTM(with) {
		t.Error("fichier installé")
	}
}
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run: `go test ./internal/rules/ -run TestPfConf -v`
Expected: FAIL — `undefined: PfConfWithWTM`.

- [ ] **Step 3: Ajouter les constantes et l'erreur**

Dans `internal/domain/constants.go` :

```go
	// The pf.conf blocks. Two, because pf orders rule types — a rdr-anchor after
	// the filter rules is a pfctl error — while load statements go last.
	ProxyPfRdrMarkStart  = "# >>> wtm rdr >>>"
	ProxyPfRdrMarkEnd    = "# <<< wtm rdr <<<"
	ProxyPfLoadMarkStart = "# >>> wtm load >>>"
	ProxyPfLoadMarkEnd   = "# <<< wtm load <<<"
	ProxyAnchorName      = "wtm"
	ProxyAnchorPath      = "/etc/pf.anchors/wtm"
	ProxyPfConfPath      = "/etc/pf.conf"
	ProxyPfRdrAnchorLine = `rdr-anchor "wtm"`
	ProxyPfLoadLine      = `load anchor "wtm" from "/etc/pf.anchors/wtm"`
	// ProxyPfRdrAnchorPrefix and ProxyPfNatAnchorPrefix locate the translation
	// section of a pf.conf we did not write.
	ProxyPfRdrAnchorPrefix = "rdr-anchor"
	ProxyPfNatAnchorPrefix = "nat-anchor"
```

Dans `internal/domain/errors.go` :

```go
	// ErrPfConfUnrecognised is returned when /etc/pf.conf has no translation
	// section to anchor into: guessing a position there would break pfctl.
	ErrPfConfUnrecognised = errors.New("/etc/pf.conf has no nat-anchor or rdr-anchor line — wtm will not guess where its rule belongs")
```

- [ ] **Step 4: Écrire l'implémentation**

`internal/rules/pfconf.go` :

```go
package rules

import (
	"strings"

	"github.com/LucasPcq/wtm/internal/domain"
)

func PfConfHasWTM(content string) bool {
	return strings.Contains(content, domain.ProxyPfRdrMarkStart)
}

// PfConfWithWTM adds the two blocks pf needs, at the two places pf accepts
// them. It refuses rather than guesses on a file with no translation section.
func PfConfWithWTM(content string) (string, error) {
	if PfConfHasWTM(content) {
		return content, nil
	}

	lines := strings.Split(content, "\n")
	at := lastIndexWithPrefix(lines, domain.ProxyPfRdrAnchorPrefix)
	if at == -1 {
		at = lastIndexWithPrefix(lines, domain.ProxyPfNatAnchorPrefix)
	}
	if at == -1 {
		return "", domain.ErrPfConfUnrecognised
	}

	rdr := []string{domain.ProxyPfRdrMarkStart, domain.ProxyPfRdrAnchorLine, domain.ProxyPfRdrMarkEnd}
	withRdr := append([]string{}, lines[:at+1]...)
	withRdr = append(withRdr, rdr...)
	withRdr = append(withRdr, lines[at+1:]...)

	return appendLoadBlock(strings.Join(withRdr, "\n")), nil
}

func PfConfWithoutWTM(content string) string {
	return dropBlock(dropBlock(content, domain.ProxyPfRdrMarkStart, domain.ProxyPfRdrMarkEnd), domain.ProxyPfLoadMarkStart, domain.ProxyPfLoadMarkEnd)
}

func appendLoadBlock(content string) string {
	block := strings.Join([]string{domain.ProxyPfLoadMarkStart, domain.ProxyPfLoadLine, domain.ProxyPfLoadMarkEnd, ""}, "\n")
	if strings.HasSuffix(content, "\n") {
		return content + block
	}
	return content + "\n" + block
}

func lastIndexWithPrefix(lines []string, prefix string) int {
	found := -1
	for i, line := range lines {
		if strings.HasPrefix(strings.TrimSpace(line), prefix) {
			found = i
		}
	}
	return found
}

func dropBlock(content, start, end string) string {
	lines := strings.Split(content, "\n")
	out := make([]string, 0, len(lines))
	inside := false
	for _, line := range lines {
		if strings.TrimSpace(line) == start {
			inside = true
			continue
		}
		if inside {
			inside = strings.TrimSpace(line) != end
			continue
		}
		out = append(out, line)
	}
	return strings.Join(out, "\n")
}
```

- [ ] **Step 5: Lancer les tests**

Run: `go test ./internal/rules/ -run TestPfConf -v`
Expected: PASS. Si `TestPfConfRemovalRestoresTheOriginalByteForByte` échoue sur un `\n` en trop, corrigez `appendLoadBlock`/`dropBlock` — c'est ce test qui garantit qu'un `uninstall` rend le fichier de départ.

- [ ] **Step 6: Commit**

```bash
git add internal/rules/pfconf.go internal/rules/pfconf_test.go internal/domain/constants.go internal/domain/errors.go
git commit -m "feat(run): insertion et retrait purs du bloc pf.conf de wtm (LUC-210)"
```

---

### Task 8 : l'interface `Redirector` et le rendu darwin

**Files:**
- Create: `internal/service/proxy/redirect.go`
- Create: `internal/service/proxy/redirect_darwin.go`
- Create: `internal/service/proxy/redirect_other.go`
- Create: `internal/service/proxy/redirect_darwin_test.go`
- Modify: `internal/domain/constants.go`, `internal/domain/errors.go`, `internal/domain/proxy.go`
- Modify: `internal/commands/run/url.go` (l'étage 2 laissé en attente en Task 6)

**Interfaces:**
- Consumes: `rules.PfConfWithWTM`/`PfConfWithoutWTM`/`PfConfHasWTM` (Task 7).
- Produces: `proxy.NewRedirector(proxy.RedirectorParams{Root string}) proxy.Redirector` ; `proxy.Plan{Files []proxy.PlannedFile, Script string}` ; `proxy.PlannedFile{Path, Content string}` ; `domain.ProxyStatus` ; `domain.ErrProxyRedirectUnsupported`.

- [ ] **Step 1: Écrire les tests qui échouent**

`internal/service/proxy/redirect_darwin_test.go` :

```go
//go:build darwin

package proxy

import (
	"os"
	"path/filepath"
	"strings"
	"testing"

	"github.com/LucasPcq/wtm/internal/domain"
)

// fakeRoot builds a temp directory standing in for /, with the stock pf.conf.
func fakeRoot(t *testing.T) string {
	t.Helper()
	root := t.TempDir()
	if err := os.MkdirAll(filepath.Join(root, "etc"), 0o755); err != nil {
		t.Fatal(err)
	}
	stock := "scrub-anchor \"com.apple/*\"\nnat-anchor \"com.apple/*\"\nrdr-anchor \"com.apple/*\"\nanchor \"com.apple/*\"\nload anchor \"com.apple\" from \"/etc/pf.anchors/com.apple\"\n"
	if err := os.WriteFile(filepath.Join(root, "etc", "pf.conf"), []byte(stock), 0o644); err != nil {
		t.Fatal(err)
	}
	return root
}

func TestPlanRendersTheAnchorWithTheBindPort(t *testing.T) {
	plan, err := NewRedirector(RedirectorParams{Root: fakeRoot(t)}).Plan(PlanParams{BindPort: 4000})
	if err != nil {
		t.Fatal(err)
	}

	anchor := fileNamed(t, plan, domain.ProxyAnchorPath)
	if !strings.Contains(anchor, "port 80 -> 127.0.0.1 port 4000") {
		t.Errorf("l'ancre doit rediriger 80 vers le port de bind:\n%s", anchor)
	}
	if !strings.Contains(anchor, "on lo0") {
		t.Errorf("la redirection est sur la loopback:\n%s", anchor)
	}
}

func TestPlanRendersThePlistWithItsLabel(t *testing.T) {
	plan, err := NewRedirector(RedirectorParams{Root: fakeRoot(t)}).Plan(PlanParams{BindPort: 4000})
	if err != nil {
		t.Fatal(err)
	}

	plist := fileNamed(t, plan, domain.ProxyPlistPath)
	if !strings.Contains(plist, domain.ProxyPlistLabel) {
		t.Errorf("le plist doit porter son label:\n%s", plist)
	}
	if !strings.Contains(plist, "RunAtLoad") {
		t.Errorf("le plist doit recharger pf au boot:\n%s", plist)
	}
}

func TestPlanRendersThePfConfWithBothBlocks(t *testing.T) {
	plan, err := NewRedirector(RedirectorParams{Root: fakeRoot(t)}).Plan(PlanParams{BindPort: 4000})
	if err != nil {
		t.Fatal(err)
	}

	conf := fileNamed(t, plan, domain.ProxyPfConfPath)
	if !strings.Contains(conf, domain.ProxyPfRdrAnchorLine) || !strings.Contains(conf, domain.ProxyPfLoadLine) {
		t.Errorf("les deux lignes doivent être là:\n%s", conf)
	}
}

func TestInspectReadsTheDeclaredStateWithoutRoot(t *testing.T) {
	root := fakeRoot(t)
	r := NewRedirector(RedirectorParams{Root: root})

	if r.Inspect().Installed {
		t.Error("racine vierge : rien n'est installé")
	}

	plan, err := r.Plan(PlanParams{BindPort: 4000})
	if err != nil {
		t.Fatal(err)
	}
	writePlanInto(t, root, plan)

	status := r.Inspect()
	if !status.Installed {
		t.Error("les trois artefacts posés : installé")
	}
	if status.BindPort != 4000 {
		t.Errorf("le port de bind se relit dans l'ancre : got %d", status.BindPort)
	}
	if !status.Supported {
		t.Error("darwin est supporté")
	}
}

func fileNamed(t *testing.T, plan Plan, path string) string {
	t.Helper()
	for _, f := range plan.Files {
		if f.Path == path {
			return f.Content
		}
	}
	t.Fatalf("le plan ne contient pas %s", path)
	return ""
}

func writePlanInto(t *testing.T, root string, plan Plan) {
	t.Helper()
	for _, f := range plan.Files {
		dest := filepath.Join(root, f.Path)
		if err := os.MkdirAll(filepath.Dir(dest), 0o755); err != nil {
			t.Fatal(err)
		}
		if err := os.WriteFile(dest, []byte(f.Content), 0o644); err != nil {
			t.Fatal(err)
		}
	}
}
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run: `go test ./internal/service/proxy/ -run 'TestPlan|TestInspect' -v`
Expected: FAIL — `undefined: NewRedirector`.

- [ ] **Step 3: Ajouter les constantes, l'erreur et le type de statut**

Dans `internal/domain/constants.go` :

```go
	ProxyPlistPath  = "/Library/LaunchDaemons/dev.wtm.proxy.plist"
	ProxyPlistLabel = "dev.wtm.proxy"
	// ProxyAnchorRuleFmt takes the privileged port then the bind port.
	ProxyAnchorRuleFmt = "rdr pass on lo0 inet proto tcp from any to 127.0.0.1 port %d -> 127.0.0.1 port %d\n"
	ProxyMechanismPf   = "pf + launchd"
	ProxyPlistFmt      = `<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key><string>%s</string>
	<key>ProgramArguments</key>
	<array><string>/sbin/pfctl</string><string>-E</string><string>-f</string><string>%s</string></array>
	<key>RunAtLoad</key><true/>
</dict>
</plist>
`
```

Dans `internal/domain/errors.go` :

```go
	// ErrProxyRedirectUnsupported is what every platform but darwin answers: the
	// named URLs keep their port there, which is a state, not a failure.
	ErrProxyRedirectUnsupported = errors.New("serving the proxy on port 80 is not implemented on this platform yet — named URLs keep their port")
```

Dans `internal/domain/proxy.go` :

```go
// ProxyStatus is what `wtm run proxy status` reports: the configured and real
// bind ports, the declared redirection, and what the probe actually saw.
type ProxyStatus struct {
	Supported      bool   `json:"supported"`
	Mechanism      string `json:"mechanism,omitempty"`
	Installed      bool   `json:"installed"`
	ConfiguredPort int    `json:"configured_port"`
	BindPort       int    `json:"bind_port,omitempty"`
	PublicPort     int    `json:"public_port"`
	Probed         int    `json:"probed_bind_port,omitempty"`
	DaemonUp       bool   `json:"daemon_up"`
	Diverged       bool   `json:"diverged"`
	ExampleURL     string `json:"example_url,omitempty"`
}
```

- [ ] **Step 4: Écrire l'interface**

`internal/service/proxy/redirect.go` :

```go
package proxy

import "github.com/LucasPcq/wtm/internal/domain"

type RedirectorParams struct {
	// Root is what every path is joined onto, "/" in production. It exists so
	// the tests never touch a system file.
	Root string
}

type PlanParams struct {
	BindPort int
}

type PlannedFile struct {
	Path    string
	Content string
}

// Plan is what Apply would write and run, rendered without writing anything —
// the recap the user confirms is this value.
type Plan struct {
	Files  []PlannedFile
	Script string
}

type Redirector interface {
	Plan(PlanParams) (Plan, error)
	Apply(PlanParams) error
	Remove() error
	Inspect() domain.ProxyStatus
}
```

- [ ] **Step 5: Écrire `redirect_other.go`**

```go
//go:build !darwin

package proxy

import "github.com/LucasPcq/wtm/internal/domain"

type unsupported struct{}

func NewRedirector(_ RedirectorParams) Redirector { return unsupported{} }

func (unsupported) Plan(PlanParams) (Plan, error)  { return Plan{}, domain.ErrProxyRedirectUnsupported }
func (unsupported) Apply(PlanParams) error         { return domain.ErrProxyRedirectUnsupported }
func (unsupported) Remove() error                  { return domain.ErrProxyRedirectUnsupported }
func (unsupported) Inspect() domain.ProxyStatus    { return domain.ProxyStatus{} }
```

- [ ] **Step 6: Écrire `redirect_darwin.go` — `Plan` et `Inspect` seulement**

```go
//go:build darwin

package proxy

import (
	"fmt"
	"os"
	"path/filepath"
	"regexp"
	"strconv"

	"github.com/LucasPcq/wtm/internal/domain"
	"github.com/LucasPcq/wtm/internal/rules"
)

type pfRedirector struct{ root string }

func NewRedirector(params RedirectorParams) Redirector {
	root := params.Root
	if root == "" {
		root = "/"
	}
	return pfRedirector{root: root}
}

func (r pfRedirector) at(path string) string { return filepath.Join(r.root, path) }

func (r pfRedirector) Plan(params PlanParams) (Plan, error) {
	current, err := os.ReadFile(r.at(domain.ProxyPfConfPath))
	if err != nil {
		return Plan{}, fmt.Errorf("read %s: %w", domain.ProxyPfConfPath, err)
	}
	conf, err := rules.PfConfWithWTM(string(current))
	if err != nil {
		return Plan{}, err
	}

	return Plan{Files: []PlannedFile{
		{Path: domain.ProxyAnchorPath, Content: fmt.Sprintf(domain.ProxyAnchorRuleFmt, domain.ProxyPrivilegedPort, params.BindPort)},
		{Path: domain.ProxyPfConfPath, Content: conf},
		{Path: domain.ProxyPlistPath, Content: fmt.Sprintf(domain.ProxyPlistFmt, domain.ProxyPlistLabel, domain.ProxyPfConfPath)},
	}}, nil
}

var anchorBindPort = regexp.MustCompile(`port (\d+)\s*$`)

func (r pfRedirector) Inspect() domain.ProxyStatus {
	status := domain.ProxyStatus{Supported: true, Mechanism: domain.ProxyMechanismPf}

	conf, err := os.ReadFile(r.at(domain.ProxyPfConfPath))
	if err != nil || !rules.PfConfHasWTM(string(conf)) {
		return status
	}
	if _, err := os.Stat(r.at(domain.ProxyPlistPath)); err != nil {
		return status
	}
	anchor, err := os.ReadFile(r.at(domain.ProxyAnchorPath))
	if err != nil {
		return status
	}

	status.Installed = true
	if match := anchorBindPort.FindStringSubmatch(string(anchor)); len(match) == 2 {
		status.BindPort, _ = strconv.Atoi(match[1])
	}
	return status
}
```

Ajoutez `Apply` et `Remove` renvoyant `nil` pour l'instant — la Task 9 les écrit.

- [ ] **Step 7: Brancher l'étage 2 laissé en attente**

Dans `internal/commands/run/url.go`, `publicProxyPort` : remplacez `Declared: false` par `Declared: proxy.NewRedirector(proxy.RedirectorParams{}).Inspect().Installed` et ajoutez l'import de `internal/service/proxy`.

- [ ] **Step 8: Lancer les tests**

Run: `go build ./... && go test ./internal/service/proxy/ ./internal/commands/run/ -count=1 -v`
Expected: PASS. Vérifiez aussi la compilation croisée : `GOOS=linux go build ./...`.

- [ ] **Step 9: Commit**

```bash
git add internal/service/proxy/redirect.go internal/service/proxy/redirect_darwin.go internal/service/proxy/redirect_other.go internal/service/proxy/redirect_darwin_test.go internal/domain/ internal/commands/run/url.go
git commit -m "feat(run): l'interface Redirector et le rendu pf/launchd de macOS (LUC-210)"
```

---

### Task 9 : `Apply` et `Remove`

**Files:**
- Modify: `internal/service/proxy/redirect_darwin.go`
- Test: `internal/service/proxy/redirect_darwin_test.go`

**Interfaces:**
- Consumes: `Plan` (Task 8).
- Produces: `Plan.Script` rempli — le script `sh` que `sudo` exécutera, testable sans jamais l'exécuter.

- [ ] **Step 1: Écrire le test qui échoue**

```go
func TestPlanScriptInstallsThenReloads(t *testing.T) {
	plan, err := NewRedirector(RedirectorParams{Root: fakeRoot(t)}).Plan(PlanParams{BindPort: 4000})
	if err != nil {
		t.Fatal(err)
	}

	for _, want := range []string{"install -m", domain.ProxyAnchorPath, domain.ProxyPfConfPath, domain.ProxyPlistPath, "launchctl bootstrap system", "pfctl -E -f"} {
		if !strings.Contains(plan.Script, want) {
			t.Errorf("le script doit contenir %q:\n%s", want, plan.Script)
		}
	}
	install := strings.Index(plan.Script, "install -m")
	reload := strings.Index(plan.Script, "pfctl -E -f")
	if install > reload {
		t.Error("les fichiers sont posés avant le rechargement de pf")
	}
}
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `go test ./internal/service/proxy/ -run TestPlanScript -v`
Expected: FAIL — `plan.Script` vide.

- [ ] **Step 3: Écrire l'implémentation**

Dans `internal/service/proxy/redirect_darwin.go`, remplissez `Script` dans `Plan` et écrivez les deux méthodes :

```go
func (r pfRedirector) Apply(params PlanParams) error {
	plan, err := r.Plan(params)
	if err != nil {
		return err
	}

	staged, err := stage(plan)
	if err != nil {
		return err
	}
	defer os.RemoveAll(staged)

	// One sudo, not three: a password prompt per file is how a user stops
	// reading what they are approving.
	cmd := exec.Command("sudo", "/bin/sh", "-c", plan.Script)
	cmd.Dir = staged
	cmd.Stdin, cmd.Stdout, cmd.Stderr = os.Stdin, os.Stdout, os.Stderr
	return cmd.Run()
}

func (r pfRedirector) Remove() error {
	conf, err := os.ReadFile(r.at(domain.ProxyPfConfPath))
	if err != nil {
		return fmt.Errorf("read %s: %w", domain.ProxyPfConfPath, err)
	}

	staged, err := stage(Plan{Files: []PlannedFile{{Path: domain.ProxyPfConfPath, Content: rules.PfConfWithoutWTM(string(conf))}}})
	if err != nil {
		return err
	}
	defer os.RemoveAll(staged)

	cmd := exec.Command("sudo", "/bin/sh", "-c", removeScript())
	cmd.Dir = staged
	cmd.Stdin, cmd.Stdout, cmd.Stderr = os.Stdin, os.Stdout, os.Stderr
	return cmd.Run()
}
```

Les deux fonctions de support, dans le même fichier :

```go
// stage writes the plan into an unprivileged temp dir under flat names; the
// script installs from there, so nothing privileged is ever handed a path the
// caller could still be writing to.
func stage(plan Plan) (string, error) {
	dir, err := os.MkdirTemp("", "wtm-proxy-")
	if err != nil {
		return "", err
	}
	for _, file := range plan.Files {
		if err := os.WriteFile(filepath.Join(dir, filepath.Base(file.Path)), []byte(file.Content), 0o644); err != nil {
			os.RemoveAll(dir)
			return "", err
		}
	}
	return dir, nil
}

func installScript(plan Plan) string {
	lines := []string{"set -e"}
	for _, file := range plan.Files {
		lines = append(lines, fmt.Sprintf("install -m 0644 %q %q", filepath.Base(file.Path), file.Path))
	}
	return strings.Join(append(lines,
		fmt.Sprintf("launchctl bootstrap system %q || true", domain.ProxyPlistPath),
		fmt.Sprintf("pfctl -E -f %q", domain.ProxyPfConfPath),
	), "\n")
}

func removeScript() string {
	return strings.Join([]string{
		"set -e",
		fmt.Sprintf("install -m 0644 %q %q", filepath.Base(domain.ProxyPfConfPath), domain.ProxyPfConfPath),
		fmt.Sprintf("launchctl bootout system %q || true", domain.ProxyPlistPath),
		fmt.Sprintf("rm -f %q %q", domain.ProxyPlistPath, domain.ProxyAnchorPath),
		fmt.Sprintf("pfctl -f %q", domain.ProxyPfConfPath),
	}, "\n")
}
```

Dans `Plan`, remplissez le champ avant de retourner : `plan.Script = installScript(plan)` (construisez d'abord la valeur, puis affectez le script, puis retournez-la). Ajoutez `"os/exec"` et `"strings"` aux imports.

- [ ] **Step 4: Lancer les tests**

Run: `go test ./internal/service/proxy/ -count=1 -v`
Expected: PASS. **Aucun test n'exécute `Apply` ni `Remove`.**

- [ ] **Step 5: Commit**

```bash
git add internal/service/proxy/redirect_darwin.go internal/service/proxy/redirect_darwin_test.go
git commit -m "feat(run): pose et retrait de la redirection derrière un sudo unique (LUC-210)"
```

---

### Task 10 : `wtm run proxy status`

**Files:**
- Create: `internal/commands/run/proxycmd/proxycmd.go`
- Create: `internal/commands/run/proxycmd/status.go`
- Create: `internal/commands/run/proxycmd/status_test.go`
- Create: `internal/output/proxy.go`
- Modify: `internal/commands/run/run.go:32`
- Modify: `internal/domain/constants.go`

**Interfaces:**
- Consumes: `proxy.NewRedirector`/`Inspect` (Task 8), `proxy.Probe` (Task 3), `rules.PublicPort` (Task 4), `domain.ProxyStatus` (Task 8).
- Produces: `proxycmd.NewCmd() *cobra.Command` ; `output.WriteProxyStatusJSON(w io.Writer, status domain.ProxyStatus) error` ; `output.ProxyStatusReport(w io.Writer, status domain.ProxyStatus)`.

- [ ] **Step 1: Écrire le test qui échoue**

`internal/commands/run/proxycmd/status_test.go` :

```go
package proxycmd

import (
	"bytes"
	"encoding/json"
	"testing"

	"github.com/LucasPcq/wtm/internal/domain"
)

func TestStatusJSONCarriesBothPorts(t *testing.T) {
	var buf bytes.Buffer
	cmd := NewCmd()
	cmd.SetOut(&buf)
	cmd.SetErr(&buf)
	cmd.SetArgs([]string{domain.CmdStatus, "--" + domain.FlagOutput, domain.OutputJSON})

	if err := cmd.Execute(); err != nil {
		t.Fatal(err)
	}

	var status domain.ProxyStatus
	if err := json.Unmarshal(buf.Bytes(), &status); err != nil {
		t.Fatalf("sortie non-JSON: %v\n%s", err, buf.String())
	}
	if status.ConfiguredPort == 0 {
		t.Error("le port configuré doit toujours être présent")
	}
}
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `go test ./internal/commands/run/proxycmd/ -v`
Expected: FAIL — le package n'existe pas.

- [ ] **Step 3: Ajouter les constantes de commande**

Dans `internal/domain/constants.go`, à côté de `CmdURL` :

```go
	CmdProxy     = "proxy"
	CmdStatus    = "status"
	CmdInstall   = "install"
	CmdUninstall = "uninstall"
```

(Réutilisez la constante existante si `CmdStatus` ou `CmdInstall` est déjà défini — vérifiez avec `grep -n 'CmdStatus\|CmdInstall' internal/domain/constants.go`.)

- [ ] **Step 4: Écrire le groupe et `status`**

`internal/commands/run/proxycmd/proxycmd.go` :

```go
// Package proxycmd implements `wtm run proxy status|install|uninstall` — the
// machine-wide redirection that lets named URLs drop their port.
package proxycmd

import (
	"github.com/spf13/cobra"

	"github.com/LucasPcq/wtm/internal/domain"
)

func NewCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   domain.CmdProxy,
		Short: "Inspect and install the redirection that serves named URLs on port 80",
		Long:  "Named job URLs carry the proxy's port unless port 80 is redirected to it. These commands report that redirection and install or remove it.",
		RunE:  runStatus,
	}
	shared.AddOutputFlag(cmd)

	cmd.AddCommand(newStatusCmd())
	cmd.AddCommand(newInstallCmd())
	cmd.AddCommand(newUninstallCmd())

	return cmd
}
```

(importez `internal/commands/shared` en plus de `cobra` et `internal/domain`.)

`internal/commands/run/proxycmd/status.go` :

```go
package proxycmd

import (
	"os"

	"github.com/spf13/cobra"

	"github.com/LucasPcq/wtm/internal/commands/shared"
	"github.com/LucasPcq/wtm/internal/domain"
	"github.com/LucasPcq/wtm/internal/output"
	"github.com/LucasPcq/wtm/internal/rules"
	"github.com/LucasPcq/wtm/internal/service/process"
	"github.com/LucasPcq/wtm/internal/service/proxy"
)

func newStatusCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   domain.CmdStatus,
		Short: "Report what actually serves named URLs on this machine",
		RunE:  runStatus,
	}
	shared.AddOutputFlag(cmd)
	return cmd
}

func runStatus(cmd *cobra.Command, _ []string) error {
	dir, err := os.Getwd()
	if err != nil {
		return err
	}
	res, err := shared.LoadConfig(cmd, dir)
	if err != nil {
		return err
	}

	status := collectStatus(rules.ProxyPort(res.Config.Global))

	if format, _ := cmd.Flags().GetString(domain.FlagOutput); format == domain.OutputJSON {
		return output.WriteProxyStatusJSON(cmd.OutOrStdout(), status)
	}
	output.Frame(cmd.OutOrStdout(), func() {
		output.ProxyStatusReport(cmd.OutOrStdout(), status)
	})
	return nil
}

func collectStatus(configured int) domain.ProxyStatus {
	status := proxy.NewRedirector(proxy.RedirectorParams{}).Inspect()
	status.ConfiguredPort = configured
	status.BindPort = configured

	socketPath := process.SocketPath()
	if process.IsDaemonRunning(socketPath) {
		resp, err := process.NewClient(socketPath).Send(process.Request{Action: process.ActionList})
		if err == nil && resp.Status != process.StatusError {
			status.DaemonUp = true
			status.BindPort = resp.ProxyPort
			status.Probed = proxy.Probe(proxy.ProbeParams{Port: domain.ProxyPrivilegedPort})
		}
	}

	status.PublicPort = rules.PublicPort(rules.PublicPortParams{
		BindPort: status.BindPort,
		Probed:   status.Probed,
		Declared: status.Installed,
		DaemonUp: status.DaemonUp,
	})
	// Declared but not reaching us: a pf flush, an OS update rewriting
	// /etc/pf.conf, or a bind port that moved since install.
	status.Diverged = status.Installed && status.DaemonUp && status.PublicPort != domain.ProxyPrivilegedPort
	return status
}
```

`internal/output/proxy.go` :

```go
package output

import (
	"fmt"
	"io"
	"strconv"

	"github.com/LucasPcq/wtm/internal/domain"
)

func WriteProxyStatusJSON(w io.Writer, status domain.ProxyStatus) error {
	return writeJSON(w, status)
}

func ProxyStatusReport(w io.Writer, status domain.ProxyStatus) {
	Section(w, domain.ProxyStatusTitle, []string{
		fmt.Sprintf(domain.ProxyStatusBindFmt, status.BindPort),
		fmt.Sprintf(domain.ProxyStatusPublicFmt, strconv.Itoa(status.PublicPort)),
		fmt.Sprintf(domain.ProxyStatusRedirectFmt, redirectState(status)),
	})
	if status.Diverged {
		Callout(w, domain.ProxyDivergedTitle, []string{domain.ProxyDivergedLine, domain.ProxyDivergedFix})
	}
}

func redirectState(status domain.ProxyStatus) string {
	if !status.Supported {
		return domain.ProxyStatusUnsupported
	}
	if !status.Installed {
		return domain.ProxyStatusNotInstalled
	}
	return fmt.Sprintf(domain.ProxyStatusInstalledFmt, status.Mechanism, status.BindPort)
}
```

Vérifiez le nom réel du helper JSON du package (`grep -n "func writeJSON\|json.NewEncoder" internal/output/*.go`) et reprenez celui qui existe plutôt que d'en ajouter un. Les chaînes `ProxyStatus*` et `ProxyDiverged*` vont dans `internal/domain/constants.go`, le callout disant que la redirection est posée mais ne mène pas au proxy qui tourne, et que `wtm run proxy install` la réécrit.

- [ ] **Step 5: Enregistrer le groupe**

Dans `internal/commands/run/run.go`, après `cmd.AddCommand(profilecmd.NewCmd())` :

```go
	cmd.AddCommand(proxycmd.NewCmd())
```

- [ ] **Step 6: Lancer les tests**

Run: `go build ./... && go test ./internal/commands/... -count=1`
Expected: PASS. Vérifiez à la main : `go run ./cmd/wtm run proxy status`.

- [ ] **Step 7: Commit**

```bash
git add internal/commands/run/proxycmd internal/output/proxy.go internal/commands/run/run.go internal/domain/constants.go
git commit -m "feat(run): wtm run proxy status dit ce qui sert réellement les noms (LUC-210)"
```

---

### Task 11 : `install` et `uninstall`

**Files:**
- Create: `internal/commands/run/proxycmd/install.go`
- Modify: `internal/commands/run/proxycmd/status_test.go`
- Modify: `internal/domain/errors.go`, `internal/domain/constants.go`

**Interfaces:**
- Consumes: `proxy.Plan`/`Apply`/`Remove` (Tasks 8-9), `components.RunStandaloneConfirm` + `components.NewConfirm` (existants).
- Produces: `domain.ErrProxyInstallNeedsTTY`.

- [ ] **Step 1: Écrire le test qui échoue**

```go
func TestInstallRefusesJSON(t *testing.T) {
	var buf bytes.Buffer
	cmd := NewCmd()
	cmd.SetOut(&buf)
	cmd.SetErr(&buf)
	cmd.SetArgs([]string{domain.CmdInstall, "--" + domain.FlagOutput, domain.OutputJSON, "--" + domain.FlagYes})

	err := cmd.Execute()

	if !errors.Is(err, domain.ErrProxyInstallNeedsTTY) {
		t.Errorf("got %v, want ErrProxyInstallNeedsTTY", err)
	}
}
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `go test ./internal/commands/run/proxycmd/ -run TestInstallRefuses -v`
Expected: FAIL — sous-commande inconnue.

- [ ] **Step 3: Ajouter l'erreur**

Dans `internal/domain/errors.go` :

```go
	// ErrProxyInstallNeedsTTY refuses a privileged write nobody is watching:
	// sudo reads a password from the terminal, so --yes cannot stand in for one.
	ErrProxyInstallNeedsTTY = errors.New("`wtm run proxy install` writes system files through sudo and needs a terminal — run it interactively")
```

- [ ] **Step 4: Écrire les deux commandes**

`internal/commands/run/proxycmd/install.go` :

```go
package proxycmd

import (
	"os"

	"github.com/spf13/cobra"
	"golang.org/x/term"

	"github.com/LucasPcq/wtm/internal/commands/shared"
	"github.com/LucasPcq/wtm/internal/domain"
	"github.com/LucasPcq/wtm/internal/output"
	"github.com/LucasPcq/wtm/internal/rules"
	"github.com/LucasPcq/wtm/internal/service/proxy"
	"github.com/LucasPcq/wtm/internal/tui/components"
)

func newInstallCmd() *cobra.Command {
	cmd := &cobra.Command{
		Use:   domain.CmdInstall,
		Short: "Redirect port 80 to the run proxy so named URLs drop their port",
		RunE:  runInstall,
	}
	cmd.Flags().BoolP(domain.FlagYes, "y", false, "Skip the confirmation (sudo still asks for a password)")
	shared.AddOutputFlag(cmd)
	return cmd
}

func runInstall(cmd *cobra.Command, _ []string) error {
	bindPort, err := requireTerminal(cmd)
	if err != nil {
		return err
	}

	redirector := proxy.NewRedirector(proxy.RedirectorParams{})
	plan, err := redirector.Plan(proxy.PlanParams{BindPort: bindPort})
	if err != nil {
		return err
	}

	output.Frame(cmd.OutOrStdout(), func() {
		output.SectionTitle(cmd.OutOrStdout(), domain.ProxyInstallRecapTitle)
		for _, file := range plan.Files {
			output.Section(cmd.OutOrStdout(), file.Path, []string{file.Content})
		}
		output.Section(cmd.OutOrStdout(), domain.ProxyInstallRecapScript, []string{plan.Script})
	})

	if yes, _ := cmd.Flags().GetBool(domain.FlagYes); !yes {
		confirmed, _ := components.RunStandaloneConfirm(components.NewConfirm(components.NewConfirmParams{
			Title:       domain.ProxyInstallConfirmTitle,
			Description: domain.ProxyInstallConfirmDesc,
			DefaultYes:  false,
		}))
		if !confirmed {
			return nil
		}
	}

	if err := redirector.Apply(proxy.PlanParams{BindPort: bindPort}); err != nil {
		return err
	}
	output.Frame(cmd.OutOrStdout(), func() {
		output.Success(cmd.OutOrStdout(), domain.ProxyInstallDone)
	})
	return nil
}

// requireTerminal refuses a privileged write nobody is watching and returns the
// bind port to redirect to.
func requireTerminal(cmd *cobra.Command) (int, error) {
	format, _ := cmd.Flags().GetString(domain.FlagOutput)
	if format == domain.OutputJSON || !term.IsTerminal(int(os.Stdin.Fd())) {
		return 0, domain.ErrProxyInstallNeedsTTY
	}

	dir, err := os.Getwd()
	if err != nil {
		return 0, err
	}
	res, err := shared.LoadConfig(cmd, dir)
	if err != nil {
		return 0, err
	}
	return rules.ProxyPort(res.Config.Global), nil
}
```

`uninstall` reprend exactement cette forme : `requireTerminal`, un récap listant `domain.ProxyAnchorPath`, `domain.ProxyPlistPath` et la ligne retirée de `domain.ProxyPfConfPath`, la même confirmation avec `domain.ProxyUninstallConfirmTitle`, puis `redirector.Remove()`. Aucun `--force` sur ni l'une ni l'autre.

Les chaînes `ProxyInstallRecap*`, `ProxyInstallConfirm*`, `ProxyInstallDone` et `ProxyUninstallConfirm*` vont dans `internal/domain/constants.go`.

- [ ] **Step 5: Lancer les tests**

Run: `go build ./... && go test ./internal/commands/... -count=1`
Expected: PASS.

- [ ] **Step 6: Vérification manuelle sur macOS**

Run: `go run ./cmd/wtm run proxy install`, lisez le récap, confirmez, saisissez le mot de passe. Puis :

```bash
sudo pfctl -a wtm -s nat          # la règle est chargée
curl -sI http://$(go run ./cmd/wtm run url | sed 's|http://||')   # 200 ou 502, jamais un refus de connexion
go run ./cmd/wtm run proxy status # installed: true, public port 80
go run ./cmd/wtm run url          # une URL sans port
go run ./cmd/wtm run proxy uninstall
diff <(sudo cat /etc/pf.conf) /etc/pf.conf.avant   # le fichier de départ, à l'octet près
```

- [ ] **Step 7: Commit**

```bash
git add internal/commands/run/proxycmd internal/domain/errors.go internal/domain/constants.go
git commit -m "feat(run): wtm run proxy install|uninstall, récap puis sudo unique (LUC-210)"
```

---

### Task 12 : le callout de `run init`

**Files:**
- Create: `internal/rules/proxyhint.go`
- Create: `internal/rules/proxyhint_test.go`
- Modify: `internal/commands/run/init.go:255-266`
- Modify: `internal/domain/constants.go`

**Interfaces:**
- Consumes: `domain.RunConfig`, `domain.ProxyStatus` (Task 8).
- Produces: `rules.ProxyInstallHintLines(rules.ProxyInstallHintParams{Config domain.RunConfig, Status domain.ProxyStatus, ExampleURL string}) []string`.

- [ ] **Step 1: Écrire le test qui échoue**

`internal/rules/proxyhint_test.go` :

```go
package rules

import (
	"strings"
	"testing"

	"github.com/LucasPcq/wtm/internal/domain"
)

func TestProxyInstallHintLines(t *testing.T) {
	published := domain.RunConfig{Jobs: []domain.JobConfig{{Name: "web", URL: &domain.JobURLConfig{Port: "PORT"}}}}
	silent := domain.RunConfig{Jobs: []domain.JobConfig{{Name: "db"}}}

	tests := []struct {
		name      string
		config    domain.RunConfig
		status    domain.ProxyStatus
		wantLines bool
		wantCmd   bool
	}{
		{name: "aucun job ne publie : rien à dire", config: silent, status: domain.ProxyStatus{Supported: true}},
		{name: "déjà installé : rien à dire", config: published, status: domain.ProxyStatus{Supported: true, Installed: true}},
		{name: "publié et pas installé", config: published, status: domain.ProxyStatus{Supported: true}, wantLines: true, wantCmd: true},
		{name: "plateforme non supportée : on informe sans proposer", config: published, status: domain.ProxyStatus{}, wantLines: true},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			lines := ProxyInstallHintLines(ProxyInstallHintParams{Config: tt.config, Status: tt.status, ExampleURL: "http://web.f.app.localhost:4000"})

			if got := len(lines) > 0; got != tt.wantLines {
				t.Fatalf("lines: got %v, want %v (%v)", got, tt.wantLines, lines)
			}
			if got := strings.Contains(strings.Join(lines, "\n"), "wtm run proxy install"); got != tt.wantCmd {
				t.Errorf("mention de la commande: got %v, want %v", got, tt.wantCmd)
			}
		})
	}
}
```

- [ ] **Step 2: Lancer le test pour vérifier qu'il échoue**

Run: `go test ./internal/rules/ -run TestProxyInstallHint -v`
Expected: FAIL — `undefined: ProxyInstallHintLines`.

- [ ] **Step 3: Ajouter les constantes de texte**

Dans `internal/domain/constants.go` :

```go
	ProxyInstallHintTitle   = "Named URLs carry a port"
	ProxyInstallHintFmt     = "Jobs publishing a url answer on %s"
	ProxyInstallHintCmd     = "`wtm run proxy install` redirects port 80 so the port disappears from the URL"
	ProxyInstallHintNoPlat  = "Dropping that port is not implemented on this platform yet"
```

- [ ] **Step 4: Écrire l'implémentation**

```go
package rules

import (
	"fmt"

	"github.com/LucasPcq/wtm/internal/domain"
)

type ProxyInstallHintParams struct {
	Config     domain.RunConfig
	Status     domain.ProxyStatus
	ExampleURL string
}

// ProxyInstallHintLines is the one place wtm mentions the redirection outside
// its own commands: `run init` informs and never acts, so this returns lines,
// not a decision.
func ProxyInstallHintLines(params ProxyInstallHintParams) []string {
	if params.Status.Installed {
		return nil
	}
	if !anyJobPublishes(params.Config) {
		return nil
	}

	lines := []string{fmt.Sprintf(domain.ProxyInstallHintFmt, params.ExampleURL)}
	if !params.Status.Supported {
		return append(lines, domain.ProxyInstallHintNoPlat)
	}
	return append(lines, domain.ProxyInstallHintCmd)
}

func anyJobPublishes(cfg domain.RunConfig) bool {
	for _, job := range cfg.Jobs {
		if job.URL != nil {
			return true
		}
	}
	return false
}
```

- [ ] **Step 5: Brancher le callout dans l'épilogue de `run init`**

Dans `internal/commands/run/init.go`, juste après le bloc `ProxyPortCollisionTitle` et avant `output.Blank` :

```go
		if lines := rules.ProxyInstallHintLines(rules.ProxyInstallHintParams{
			Config:     outcome.Config,
			Status:     proxy.NewRedirector(proxy.RedirectorParams{}).Inspect(),
			ExampleURL: exampleNamedURL(outcome.Config, res, proxyPort),
		}); len(lines) > 0 {
			output.Callout(cmd.ErrOrStderr(), domain.ProxyInstallHintTitle, lines)
		}
```

`exampleNamedURL` construit l'URL du premier job publiant, avec `rules.RouteHost` et `rules.JobOrigin` — le worktree courant et `filepath.Base(res.ProjectDir)` comme projet.

- [ ] **Step 6: Lancer les tests**

Run: `go build ./... && go test ./internal/rules/ ./internal/commands/run/ -count=1`
Expected: PASS, y compris `init_test.go`.

- [ ] **Step 7: Commit**

```bash
git add internal/rules/proxyhint.go internal/rules/proxyhint_test.go internal/commands/run/init.go internal/domain/constants.go
git commit -m "feat(run): run init signale que les URLs nommées portent un port (LUC-210)"
```

---

### Task 13 : documentation, skill agent et validation

**Files:**
- Modify: `docs/` (généré)
- Modify: `README.md`
- Modify: `internal/commands/agents/assets/using-wtm.skill.md`

**Interfaces:**
- Consumes: toutes les tâches précédentes.
- Produces: rien de consommé par du code.

- [ ] **Step 1: Régénérer la référence**

Run: `make docs`
Expected: de nouveaux `docs/wtm_run_proxy*.md`. `docs/dev/` doit être intact.

- [ ] **Step 2: Mettre à jour le tableau du README**

Ajoutez une ligne au tableau d'aperçu, dans la section qui groupe déjà `wtm run …`, pointant vers `docs/wtm_run_proxy.md`. **Ne rajoutez aucun tableau de flags** — `--help` et `docs/` font foi.

- [ ] **Step 3: Mettre à jour la skill agent**

Dans `internal/commands/agents/assets/using-wtm.skill.md` : les trois nouvelles commandes, et surtout — c'est le point qui casse un agent qui parse la sortie — **`wtm run url` peut désormais renvoyer une URL sans port** quand la redirection est installée. Mentionnez aussi que `run proxy install` exige un terminal et ne peut pas être appelé en mode agent.

- [ ] **Step 4: Valider le projet**

Lancez le sous-agent **`build-validator`**. Il vérifie compilation, `go vet`, l'analyse statique et la suite de tests, plus la règle d'imports de `internal/flow/`.
Expected: aucun problème bloquant.

- [ ] **Step 5: Vérifier la compilation croisée**

Run: `GOOS=linux go build ./... && GOOS=windows go build ./...`
Expected: succès — `redirect_other.go` couvre les deux.

- [ ] **Step 6: Commit**

```bash
git add docs README.md internal/commands/agents/assets/using-wtm.skill.md
git commit -m "docs(run): référence, README et skill agent pour wtm run proxy (LUC-210)"
```

---

## Ce que ce plan ne fait pas

Hors périmètre, conformément à la spec : HTTPS et la CA locale ; l'implémentation Linux (`setcap` / `sysctl` / `iptables`) ; le réglage `[run] addressing` et la réécriture des `.env` en origines (LUC-212) ; toute modification du scan de port de LUC-209.

LUC-212 récupère de ce plan : `rules.JobOrigin` (Task 1), un port public résoluble sans daemon (Tasks 4 et 8), et un port public stable par construction quand la redirection est posée.
